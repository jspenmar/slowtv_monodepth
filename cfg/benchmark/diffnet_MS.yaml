# -----------------------------------------------------------------------------
net:
  depth:
    dec_name: 'diffnet'
# -----------------------------------------------------------------------------
loss:
  img_recon:
    use_min: True
    use_automask: True
# -----------------------------------------------------------------------------
dataset:
  kitti_lmdb:
    supp_idxs: [ -1, 1, 0 ]
# -----------------------------------------------------------------------------