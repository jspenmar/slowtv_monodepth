# -----------------------------------------------------------------------------
net:
  depth:
    dec_name: 'monodepth'
# -----------------------------------------------------------------------------
loss:
  img_recon:
    use_min: False
    use_automask: False

  disp_smooth: ~
# -----------------------------------------------------------------------------
dataset:
  kitti_lmdb:
    supp_idxs: [ -1, 1, 0 ]
# -----------------------------------------------------------------------------